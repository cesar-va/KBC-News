<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/compartir"
        android:title="Compartir"
        android:icon="@android:drawable/ic_menu_share"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/empty"
        android:title="Options"
        android:orderInCategory="101"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_overflow">

        <menu>

            <item android:id="@+id/favoritos"
                android:icon="@android:drawable/ic_input_add"
                app:showAsAction="ifRoom"
                android:title="Agregar a favoritos" />

            <item android:id="@+id/copiar"
                android:icon="@android:drawable/ic_input_get"
                app:showAsAction="ifRoom"
                android:title="Copiar enlance" />

            <item android:id="@+id/navegador"
                android:icon="@drawable/ic_open_in_browser"
                app:showAsAction="ifRoom"
                android:title="Abrir en el navegador" />

        </menu>
    </item>
</menu>